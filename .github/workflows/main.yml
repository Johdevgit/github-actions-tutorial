name: Mi primer Workflow

on:
  push:
    branches:
      - qa

jobs:
  ejemplo:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar API Key (No se mostrará por seguridad)
        run: |
          echo "Repositorio: ${{ github.repository }}"
          echo "Actor: ${{ github.actor }}"
          echo "Evento: ${{ github.event_name }}"